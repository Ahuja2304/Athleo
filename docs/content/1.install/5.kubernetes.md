::alert{type="warning"}
**Community Contribution:** The Kubernetes and Helm chart support are community-provided. The Athleo maintainers do not currently use Kubernetes and cannot provide full review or official support for this installation method.
::

## Quick Start (Kubernetes)

Deploy Athleo on Kubernetes using the Helm chart directly from the repository.

```bash
# 1. Install with default settings (bundled PostgreSQL, no ingress)
helm install athleo oci://ghcr.io/codewithcj/charts/athleo

# -- OR install directly from source --
git clone https://github.com/CodeWithCJ/SparkyFitness.git
helm install athleo ./Athleo/helm/chart

# 2. (Optional) Customize values
helm install athleo ./Athleo/helm/chart -f my-values.yaml

# 3. Access the application in browser via Ingress or HTTPRoute you've specified in values
```

For all configuration options (external database, ingress, OIDC, email, etc.) see the [Helm chart README](https://github.com/CodeWithCJ/SparkyFitness/blob/main/helm/README.md).
